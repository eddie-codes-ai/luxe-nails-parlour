// src/app/admin/artist-schedules/page.tsx
import ArtistSchedulesClient from './ArtistSchedulesClient'

export const metadata = {
  title: 'Artist Schedules | LuxeNails Admin',
}

export default function ArtistSchedulesPage() {
  return <ArtistSchedulesClient />
}